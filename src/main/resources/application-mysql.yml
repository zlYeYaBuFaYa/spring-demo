spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # MySQL数据库连接配置
    # 参数说明：
    # - useUnicode=true&characterEncoding=utf8：支持中文
    # - useSSL=false：开发环境禁用SSL
    # - serverTimezone=Asia/Shanghai：设置时区为上海
    # - allowPublicKeyRetrieval=true：允许客户端从服务器获取公钥
    url: jdbc:mysql://8.148.219.121:33067/spring_demo?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 3Zlac397-bae9-4019-809b-dD2b6dd54d24
    # HikariCP 连接池配置
    hikari:
      maximum-pool-size: 10              # 最大连接数
      minimum-idle: 5                    # 最小空闲连接
      connection-timeout: 30000          # 连接超时时间（30秒）
      idle-timeout: 600000               # 空闲连接超时时间（10分钟）
      max-lifetime: 1800000              # 连接最大生命周期（30分钟）
      connection-test-query: SELECT 1   # 测试连接的SQL
      validation-timeout: 3000           # 验证连接超时时间（3秒）
